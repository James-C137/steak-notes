---
import Card from './Card.astro';

interface Props {
  type?: 'info' | 'warning' | 'tip';
  title?: string;
}

const { type = 'info', title } = Astro.props;

const styles = {
  info: { background: 'bg-sand', borderColor: 'border-charcoal' },
  warning: { background: 'bg-coral/20', borderColor: 'border-ochre' },
  tip: { background: 'bg-cream', borderColor: 'border-ochre' },
};
---

<Card
  as="aside"
  background={styles[type].background}
  borderColor={styles[type].borderColor}
  margin="my-8"
>
  {title && <p class="font-black text-lg mb-2">{title}</p>}
  <div class="prose prose-sm">
    <slot />
  </div>
</Card>
